/* 优先级规则测试 */
#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    int top, score;
    top = score = -(2 + 5) * 6 + (4 + 3 * (2 + 3));
    printf("top = %d \n", top);

    return 0;
}
