const double * pt : 
    指向常量的指针, 不可以通过这个指针pt来修改它所指向的对象(变量)
    但可以让它指向别处(无论它指向哪, 都不能通过这个它来修改它所指向的对象(变量))

double * const pt : 
    指针是一个常量, 指针pt指向的位置是不能改变的

const double * const pt : 
    pt指针指向的位置不能修改, 同时也不能通过pt指针来修改其所指向的对象(变量)